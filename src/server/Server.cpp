//#include "Server.h"
#include <fstream>
#include <stdlib.h>
#include <stdio.h>
#include <engine/Engine.h>

namespace server {

/*    Server::Server(engine::Engine* eng, std::thread* gamesThread, state::State CurrentState, std::vector<Command*> serverCommands) {
    }
    
    void Server::join (){
       
    }*/
    
    /*HttpStatus UserService::get (Json::Value& out, int id) const {
    const User* user = userDB.getUser(id);
    if (!user)
        throw ServiceException(HttpStatus::NOT_FOUND,"Invalid user id");
    out["name"] = user->name;
    out["age"] = user->age;
    return HttpStatus::OK;
}*/
    
}